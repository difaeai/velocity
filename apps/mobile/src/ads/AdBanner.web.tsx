/**
 * Web stub for the banner — the Google Mobile Ads SDK is native-only.
 *
 * Same pattern as LiveMap.web.tsx: importing the real module under
 * react-native-web would throw at bundle time, so the web build gets a
 * component that renders nothing.
 */
export function AdBanner(_props: { style?: object; hideOnKeyboard?: boolean }) {
  return null;
}
